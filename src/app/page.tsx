// Phase 0 스텁 홈. 검색→결과→변환 UI는 Phase 5(app/page.tsx)에서 배선한다.
export default function Home() {
  return (
    <main>
      <h1>mp3tuber</h1>
      <p>scaffolding ready — UI는 이후 phase에서 구현됩니다.</p>
    </main>
  );
}
